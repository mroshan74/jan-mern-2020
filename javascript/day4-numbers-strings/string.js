// str -> str object -> property and methods

const name = 'dct'  // "dct"

console.log(name) // 'dct'
console.log(name[0]) // 'd'
console.log(name[1]) // 'c'
console.log(name[3]) // 't'

console.log(name.length) // 3
console.log(name.toUpperCase()) //'DCT'